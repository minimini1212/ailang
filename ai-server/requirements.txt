fastapi==0.115.0
uvicorn[standard]==0.30.6
python-dotenv==1.0.1

# 설정 읽기 — config.py 가 직접 import 한다.
# ⚠️ 예전엔 langchain-community 를 타고 전이 설치돼 «우연히» 돌고 있었다.
pydantic-settings==2.5.2

# LangChain
langchain==0.3.0
# 🔄 2026-09-01: langchain-google-genai → langchain-openai
#    GitHub Models 가 OpenAI 호환 규격이라 같은 클라이언트를 그대로 쓴다.
#    근거와 대가: docs/rules/ai-call-policy.md
langchain-openai==0.2.2

# Redis
redis==5.0.8

# JWT 검증 (Spring Boot 발급 토큰)
python-jose[cryptography]==3.3.0

# HTTP 클라이언트
httpx==0.27.2

# ⬜ Phase 3(RAG) 에서 되살린다 — 지금은 읽는 코드가 한 줄도 없다.
#    설치해 두면 «이미 구현돼 있다»고 오해하게 만들어서 뺐다.
# supabase==2.7.4
# langchain-community==0.3.0
