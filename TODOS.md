# TODOS — 남은 일 정본

> **이 파일에는 「남은 것」만 적는다.** 무엇을 했나·무엇을 쟀나·왜 깨졌나는
> `docs/reports/daily/` 로 간다. 끝난 항목은 주석을 붙이는 것이 아니라 **한 줄로 접어**
> 맨 아래 `✅ 끝난 것` 표로 옮기고, 상세는 정본 문서로 링크한다.
>
> 🔴 **체크 안 된 상자는 근거가 아니다.** `[ ]` 를 보고 일을 시작하기 전에 코드로 확인할 것.
> `PROJECT_PLANNER.md` 는 Phase 2 를 완료로 표시해 두고, 실제로 머지돼 있던 정답 공개 API·
> 자가채점·진단 테스트·AI 모의문제는 아예 언급하지 않았다.
>
> ⚠️ **800줄이 신호다.** 그보다 길어졌다면 이야기가 새어 들어온 것이니 덧붙이기 전에 쳐낸다.
> 지운 것은 `git log -- TODOS.md` 로 되살릴 수 있다.

---

## 🚦 지금 어디에 있나

**기능은 Phase 2 까지 돌아간다. 그런데 그 기능의 근거가 되는 숫자를 믿을 수 없다.**

정답률이 난이도를 정하고, 난이도가 다음 문제를 정한다. 그 정답률을 지금은 **클라이언트가
정한다.** 그래서 다음 할 일은 새 기능(RAG·관리자 API)이 아니라 **1절과 2절**이다.

🔴 **1절·2절을 끝내기 전에 Phase 3(RAG)에 손대지 않는다.** RAG 를 붙이면 코드가 늘고,
늘어난 코드 위에서 같은 결함을 고치는 것이 더 비싸다.

### 🎯 화면이 아직 「고리」를 안 열었다 (2026-08-27 프론트 확인)

프론트엔드는 `C:\webStorm_workspace\ailang` 에 있다 — **별도 git 저장소**(2026-09-11 부터).
화면이 다섯 개뿐이고, **맞춤 문제·개념 설명·AI 모의문제 화면이 없다** —
`/adaptive` · `/random` · `/random-by-grade` · `/{id}/concept` · `/ai-generated`
**다섯 엔드포인트가 아무 데서도 안 불린다.**

학생이 문제를 푸는 유일한 경로는 **진단 테스트 20문제**(`Assessment.tsx`)다.

- 🎯 **그래서 지금이 고칠 때다.** 화면을 붙이고 나면 조작 가능한 고리가 열린 채로 커진다.
- 🔄 **2026-09-09: 화면을 붙일 준비가 됐다.** 챕터가 8개가 되어 맞춤 문제가 실제로
  「맞춤」이 되고, 난이도가 없는 칸에서도 404 가 안 난다.
- ⚠️ 다만 진단 테스트도 `submitAnswer` 를 부르므로 **통계는 이미 갱신되고 있다.**
  20문제가 여러 챕터에 흩어져 각 챕터에 1~2건씩만 남고, `totalCount < 3` 이라 난이도는
  안 움직이면서 **정답률 분모만 오염된다.** 진단 결과를 초기 난이도로 쓸지는 8절 참고.

### 순서

```
✅ 0. 측정             실측 완료 → docs/research/aihub-data-measurement-2026-08-31.md
✅ 1. 인증 경계        typ 클레임 · 챗봇 신원 · 로그아웃  (Redis·인증코드는 남음)
✅ 2. 채점 정확성      정규화 · 정답 공개 · selfJudge · 소속검증 · 반복제출
✅ 3. 장애 내성        타임아웃 · 트랜잭션 · 429 · 싱글턴
✅ 4. 적재 정합성      가드 · 사유별 실패 · 순서 결정성 · 유니크 제약
✅ 4.5 챕터 정리       유형 331개 → 대단원 8개. 난이도 고리가 실제로 돈다
🔄 5. 화면            ✅ 맞춤 문제 · 개념 설명 (2026-09-10)   ⬜ AI 모의문제는 남음
✅ 6. 경계 굳히기      인증 메일·코드 상한 · 포트를 이 PC 로 · 검사 73건 (2026-09-15)

🔴 다만 0절(난이도 «축»)은 **사용자 결정 없이는 못 닫는다.**
```

> 🔄 **2026-09-15: 목록을 코드와 전부 대조했다.** 열린 항목 40개 중 **4개가 이미 끝나
> 있었다** (복수정답 적재 · 적재 경로 · 챕터 유니크 제약 · requirements 누락). 지웠다.
> 경위: [`docs/reports/daily/REPORT_2026-09-15.md`](docs/reports/daily/REPORT_2026-09-15.md)

리뷰 원문: [`docs/review/gstack/`](docs/review/gstack/) ·
[`docs/review/oh-my-claudecode/`](docs/review/oh-my-claudecode/)

---

## 0. 🔴 난이도·챕터 매핑 — 실측 결과 여기가 1순위가 됐다

정본: [`docs/research/aihub-data-measurement-2026-08-31.md`](docs/research/aihub-data-measurement-2026-08-31.md)

- [ ] **난이도 축을 «무엇으로» 정할 것인가** — ⚠️ **사용자 결정**이다 (8절).
      🔄 **2026-09-07: 급한 불은 껐다.** 지금 DB 를 재현 못 하던 문제는
      「원본 id → 난이도」 스냅샷 1,132줄을 기록해 닫았다 (**재현율 99.8%**).
      🔴 **다만 이건 규칙이 아니라 기록이다.** 새 학년 자료에는 그 id 가 없어
      기존 매핑으로 떨어지고, 그러면 **상(HIGH) 이 한 건도 안 나온다.**
      → 근거·후보 비교: [`docs/research/difficulty-origin-2026-09-07.md`](docs/research/difficulty-origin-2026-09-07.md)
### ✅ 이번에 닫힌 것 (2026-09-09)

| 무엇 | 결과 |
| --- | --- |
| 챕터 331개 · 60%가 문제 3개 이하 | **대단원 8개**로 묶었다. 가장 작은 챕터도 76문제 — 3문제 문턱이 사라졌다 |
| `order_num` 이 사실상 순서가 아님 | 챕터 8개에 순번 1~8 이 하나씩 |
| 유형 331종이 사라질 뻔함 | `PROBLEMS.TOPIC` 으로 옮겼다. 개념 설명에 `단원 · 유형` 을 함께 보낸다 |
| 🔴 **맞춤 문제가 404 를 냄** | 챕터에 그 난이도가 0건일 때 폴백. 검사 4건 추가 |
| `CHAPTERS(grade,title)` 유니크 제약 없음 | 걸었다 (`UK_CHAPTERS_GRADE_TITLE`) |
| 원본 id 컬럼이 없어 적재가 전부 아니면 전무 | `PROBLEMS.SOURCE_ID` (UNIQUE) 신설 |
| Docker 빌드가 캐시 오류로 계속 죽음 | `build`·`.gradle` 을 볼륨으로 뺐다 — 증분 빌드가 산다 |

- [ ] 🔴 **정답이 200자를 넘는 문제 20건이 조용히 빠진다** — 🆕 2026-09-10 실측으로 찾았다.
      `PROBLEMS.ANSWER` 가 `VARCHAR2(200)` 인데 원본의 `answer_bbox` 에 **정답과 풀이가 함께**
      들어 있는 건이 있다(최대 1,106바이트). 저장에서 터져 `LoadOutcome.ERROR` 로 버려진다.
      🔴 **처음부터 그랬다** — 예전 DB 도 1,132건이었고 아무도 몰랐다.
      ⚠️ 빠지는 것이 대부분 **서술형·표가 있는 문제**라 자료가 치우친다.
      ⚠️ **대응은 사용자 결정이다**(8절) — 컬럼을 넓힐지(CLOB) · 정답만 잘라 담을지 · 버릴지는
      **채점 방식과 얽힌 판단**이다.
      → 근거: [`docs/research/reload-result-2026-09-10.md`](docs/research/reload-result-2026-09-10.md) §2
- [ ] **`(챕터×난이도)` 24칸 중 «10칸»이 비어 있다** — 🔄 **2026-09-15 DB 직접 조회로
      재확인**(2026-09-10 적재 기록과 일치). 난이도가 단원별로 몰려 있다 —
      정수와 유리수는 **165건 전부 하**, 입체도형은 **168건 전부 상**, 좌표평면·기본도형은
      전부 중이다. 🎯 폴백으로 **고장은 막았지만**(`DifficultyFallback`),
      근본은 위의 「난이도 축」 결정이다.
- [ ] 🔴 **객관식 500문제 중 14문제는 «맞힐 수 없다»** — 🆕 2026-09-15 실측으로 찾았다.
      정답 칸에 보기 번호가 아니라 **풀이 전문**이 들어가 있다
      (`① 어떤 수를 $a$ 라고 하면 … \therefore a=\frac{1}{6}`). 채점은 보기 번호를
      맞춰 보므로 **학생이 무엇을 골라도 오답**이다.
      → 모양별 분포: 보기번호 429 · 12자 이하 57 · **12자 초과 14**
      ⚠️ **대응은 사용자 결정이다**(8절) — 풀이를 버릴지 해설 칸으로 옮길지.

## 1. 채점을 서버로 되돌리기

정본: [`docs/rules/grading-and-difficulty.md`](docs/rules/grading-and-difficulty.md)

- [ ] **자가채점 자체를 재검토** — 단답형 채점을 클라이언트에 맡길지, 서버가 채점하고
      이의제기를 따로 받을지. ⚠️ **사용자가 정할 일**이다 (8절).
      🎯 지금은 값이 없으면 400 으로 거부하므로 «조용히 오답» 은 없어졌다. 다만
      학생이 「맞았다」고 보내면 그대로 통계에 들어가는 것은 그대로다.
- [ ] **복수정답의 순서** — `①,③` 과 `③,①` 이 지금은 다른 답이다. 원본에 복수정답이
      6건 있는데 순서까지 맞아야 하는지 안 정했다. 지금 동작은 검사로 못 박아 뒀다.

### ✅ 이번에 닫힌 것 (2026-09-15)

| 무엇 | 결과 |
| --- | --- |
| 🔴 **2026-09-11 의 「고쳤다」가 사실이 아니었다** | 실패를 **그 실패가 일어난 트랜잭션 «안»** 에서 삼켰다. 삼켜도 그 트랜잭션은 이미 「롤백 전용」이라 **반환하며 커밋할 때 다시 터졌다** — 학생이 보는 것은 똑같은 500 이었고, 예외 이름만 바뀌어 있었다. 잡는 자리를 바깥으로 옮겼다 → 새 규칙 R10-1 |

⚠️ **나흘 동안 「닫힘」으로 적혀 있었다.** 실제 DB 로 재 보기 전까지 아무도 몰랐다 —
「고쳤지만 확인 못 함」을 「고쳤다」로 읽으면 이렇게 된다.

### ✅ 이번에 닫힌 것 (2026-09-11)

| 무엇 | 결과 |
| --- | --- |
| 🔴 동시 제출이 유일 제약을 깨 **500** | 「잠금 → 이력조회 → 기록」 순서로 못 박았다. 행 «만들기» 는 별도 트랜잭션 (`UserChapterStatsCreator`) → 규칙 R10 |
| 정답률 한 건이 사라짐 (lost update) | 통계를 고쳐 쓸 때 `SELECT ... FOR UPDATE` 로 잡는다 |
| 같은 문제를 「첫 제출」로 두 번 셈 | 첫 제출 판정을 **잠금 뒤로** 옮겼다 — 앞에 두면 잠금이 있어도 두 번 세어진다 |

🔄 **2026-09-15: 검사가 생겼다** (`SubmitAnswerConcurrencyTest`, 2건). 실제 DB 가 필요하고,
DB 가 없으면 **조용히 통과하지 않고 건너뛴다.**

### ✅ 이번에 닫힌 것 (2026-09-02)

| 무엇 | 결과 |
| --- | --- |
| 정규화가 다른 답을 같게 만듦 | 모르는 명령어를 지우지 않는다. `√2`≠`2`, `8π`≠`8` |
| `"$"` 한 글자로 정답 처리 | 정규화 결과가 비면 비교하지 않는다 |
| 객관식 정답이 제출 전에 나감 | 정답 공개는 `SHORT_ANSWER` 만 |
| `selfJudge` 누락이 오답으로 기록 | 400 으로 거부한다 |
| 문제-챕터 소속 미검증 | 관계를 증명한 뒤에 통계를 건드린다 |
| 반복 제출로 정답률 조작 | 통계에는 **문제당 첫 제출**만. 이력은 계속 쌓는다 |
| 채점 로직에 검사를 못 붙임 | `AnswerNormalizer` 로 꺼냈다 — 순수 함수 |
| 🎯 **인프라 없이 도는 검사가 0개** | **15건 추가.** 옛 코드에서 5건이 빨간불인 것을 확인했다 |

## 2. 인증 경계 🔴 지금 할 일

- [ ] 🔴 **`.env` 에 `REDIS_PASSWORD` 를 채운다** — ⚠️ **사용자가 할 일이다**
      (Claude 는 `.env` 를 쓰지 않는다). 배선은 2026-09-15 에 끝났다: compose 가 이 값을
      `requirepass` 로 넘기고 두 서버가 같은 값으로 붙는다. **비어 있으면 저장소는
      예전처럼 인증 없이 뜬다** — 거기엔 리프레시 토큰 원문이 들어 있다.
      ⚠️ 값을 넣은 뒤 `docker compose up -d redis` 로 컨테이너를 **다시 만들어야** 적용된다.
      → 키 목록: [`docs/DATA_CONTRACT.md`](docs/DATA_CONTRACT.md) §5.5
- [ ] **`8001` 포트를 아예 안 열 수 있나** — 🔄 2026-09-15 에 `127.0.0.1` 로 좁혀
      같은 망에서는 안 보이게 했다. 완전히 닫으려면 스프링도 compose 안으로 들어와야 하고,
      그건 **배포 대상 결정**에 달려 있다 (8절).
- [ ] **한 계정이 한 기기에서만 된다** — 리프레시 토큰이 이메일 하나로만 저장돼 두 번째
      기기 로그인이 첫 기기를 튕겨낸다. ⚠️ 의도인지부터 정한다 (8절).

### ✅ 이번에 닫힌 것 (2026-09-15 · 오후)

| 무엇 | 결과 |
| --- | --- |
| **프론트 저장소의 원격** | 🎯 **두지 않기로 했다 — 사용자 결정 (2026-09-15).** 화면 코드는 로컬 git 으로만 관리한다. ⚠️ 따라서 **백업은 없다**: 그 디스크가 죽으면 화면 코드는 사라지고, 이 저장소의 어떤 커밋으로도 되살릴 수 없다. 결정이 바뀌면 `git remote add` 한 줄이면 된다 |
| **「FastAPI 도 로그아웃을 알게 하기」** | 🎯 **구현할 것이 아니라 이미 성립하는 성질이었다.** FastAPI 는 `typ=SERVICE` 만 받으므로 블랙리스트에 오르는 «학생 액세스 토큰» 이 애초에 도달하지 못한다. 남은 경로(Spring 이 대신 부르는 것)는 Spring 필터가 먼저 막는다. **그 성질을 검사로 못 박고**(`JwtAuthenticationFilterTest`) 항목을 접었다 |
| 🔴 **관리자 경로가 열릴 «준비» 가 돼 있었다** | `/api/admin/**` 를 `hasRole("ADMIN")` 으로 **미리** 닫았다. 엔드포인트는 아직 없어서 오늘은 아무것도 안 막지만, 없으면 누가 관리자 API 를 처음 추가하는 순간 로그인한 학생 전원에게 열린다 |
| 🔴 **`GET /api/chapters` 가 500 을 두 가지로 냈다** | 비로그인+학년없음 → **401**, 어휘 밖 학년 → **400**. 둘 다 원인은 학생 쪽인데 「서버 내부 에러」로 나가고 있었다. 계약 문서에 **적혀만 있고 안 고쳐진 채**였다 |
| 로그아웃 관문에 검사가 0건이었다 | **12건.** 블랙리스트 확인이 «인증을 채우기 전» 에 오는지, 키가 글자까지 같은지까지 본다 |

### ✅ 이번에 닫힌 것 (2026-09-15 · 오전)

| 무엇 | 결과 |
| --- | --- |
| 🔴 **인증 메일을 무한히 쏠 수 있었다** | 한 주소·창 하나당 상한. 상한 확인이 **메일 발송보다도, 가입 여부 조회보다도 앞**이다 — 뒤에 두면 각각 메일 폭탄과 가입자 명단 조회가 안 막힌다 |
| 🔴 **6자리 코드를 무제한 대입할 수 있었다** | 입력 횟수를 **대조 전에** 센다. 상한을 넘으면 **코드를 버린다** — 안 버리면 기다렸다 이어서 넣으면 된다 |
| 「틀렸다」와 「너무 많이 틀렸다」가 같은 응답 | 400 / 429 로 나눴다. 앞은 다시 넣으면 되고 뒤는 **다시 받아야** 한다 |
| Oracle·Redis·AI 서버가 **모든 인터페이스**에 열려 있었다 | 셋 다 `127.0.0.1` 로 묶었다 — 같은 와이파이에서 안 보인다 |
| Redis 에 비밀번호를 걸 수 없었다 | compose 가 `REDIS_PASSWORD` 를 `requirepass` 로 넘긴다 (⚠️ 값은 위 열린 항목) |

⚠️ **상한 «값» 은 잠정값이다** — 창 1시간에 5통 · 코드 하나에 5회. 설정으로 빠져 있어
재배포 없이 바뀐다 (8절).

### ✅ 이번에 닫힌 것 (2026-09-01 · 2026-09-11)

| 무엇 | 결과 |
| --- | --- |
| **학년을 고칠 길이 없었다** (09-11) | `PATCH /api/users/me/grade` + 마이페이지 학년 변경. 400 을 받은 학생이 갈 곳이 생겼다 → [API_CONTRACT](docs/API_CONTRACT.md) §3 |
| 리프레시 토큰이 액세스로 통함 | 토큰에 `typ` 클레임(ACCESS/REFRESH/SERVICE). 필터가 ACCESS 만 받는다 |
| 유출된 리프레시가 로그아웃 후에도 7일 유효 | 위와 같이 닫힘 — 인증에 못 쓴다 |
| 서비스 토큰과 학생 토큰이 구분 안 됨 | `createServiceToken`. FastAPI 는 SERVICE 만 받는다 |
| 학생이 8001 을 직접 호출 가능 | 학생 토큰이 401 로 막힌다 (5종 검증) |
| **챗봇 세션이 유저에 안 묶임** | Spring 이 인증된 학생 id 를 실어 보낸다. **실제 시나리오로 안 새는 것 확인** |
| 로그아웃이 실패를 삼킴 | 빈 catch 제거. Redis 가 죽으면 503 으로 답한다 |

⚠️ **기존 로그인 세션은 전부 끊긴다.** `typ` 없는 옛 토큰을 거부하기 때문이다.
받아 주면 구분이 없던 시절 토큰이 그대로 통과해 고친 의미가 없어진다.

## 3. AI 호출 안정성 🟠

정본: [`docs/rules/ai-call-policy.md`](docs/rules/ai-call-policy.md)

🔄 **2026-09-01: 제공자를 Google AI Studio 로 확정했다** (`gemini-2.5-flash`).
모델은 원래 쓰던 것 그대로이고, 붙이는 방식만 OpenAI 호환으로 바꿨다.
⚠️ GitHub Models 를 붙이려다 **이미 종료된 서비스**여서(410 Gone) 되돌렸다 —
전말: [`docs/rules/ai-call-policy.md`](docs/rules/ai-call-policy.md) §0.5

- [ ] 🔴 **사용자별 AI 호출 상한** — 지금 상한이 **하나도 없다.** 학생 한 명이
      「AI 모의문제」를 50번 누르면 **그날치 전부가 사라지고**, 누가 태웠는지 셀 방법도 없다.
      `PROBLEMS` 에 행만 계속 쌓인다. ⚠️ **상한 값은 사용자가 정할 일이다** (8절).
- [ ] **AI 응답이 느리다** — 실측 개념설명 **17~19초** · 모의문제 **13초**.
      `docs/PRD.md` §4 의 「AI API ≤ 5초」를 3배 넘는다. 타임아웃을 60초로 잡아 끊기지는
      않지만, 학생이 20초를 기다린다. 프롬프트를 줄일지 더 작은 모델을 쓸지 정해야 한다.
- [ ] **한도 초과가 실제로 429 로 오는지** — 아직 한도를 못 채워 봐서 확인 못 했다.

### ✅ 이번에 닫힌 것 (2026-09-01)

| 무엇 | 결과 |
| --- | --- |
| AI 실패가 500 한 덩어리 | FastAPI 가 429·502·503 으로 나눠 내린다 |
| 생성 결과를 저장 전 검증 안 함 | 보기 1000자·정답 200자·유형·보기번호를 검사한다 |
| JSON 파싱이 취약 | 펜스 2개·설명 문장·JSON 아님을 전부 처리한다 |
| 요청마다 클라이언트·커넥션 생성 | 모듈 수준 1회 생성으로 바꿨다 |
| 챗봇 이력 무한 증가 + TTL 유실 | 10쌍 상한(`ltrim`) + 파이프라인 한 번에 |
| 챗봇에 시스템 프롬프트 없음 | 수학 교사 역할·수준·수식 규칙을 넣었다 |
| 모르는 난이도를 「중간」으로 바꿔치기 | 거부한다 |
| Supabase 값이 없으면 서버가 안 뜸 | 선택 필드로 바꿨다 |
| Spring 이 AI 실패를 전부 500 으로 덮음 | `AiServerClient` 가 429·502·503 을 보존한다 |
| `RestTemplate` 타임아웃 없음 | 연결 5초 · 읽기 60초 (설정으로 뺐다) |
| `getAiProblem` 이 트랜잭션 안에서 13초 HTTP 호출 | `AiProblemStore` 로 분리, `NOT_SUPPORTED` |
| 실패가 «개념 설명»·«답변» 문장으로 둔갑 | 실패는 실패로 던진다 |
| JSON 안 LaTeX 백슬래시가 제어문자로 깨짐 | 프롬프트 + 재시도 + 복원 3중 (실호출 확인) |
| 모델이 정답을 숫자로 줘서 500 | 저장 전 타입을 맞춘다 |

## 4. 설정·환경 🟡

- [ ] **springdoc 도입 검토** — [`docs/API_CONTRACT.md`](docs/API_CONTRACT.md) 를 손으로
      관리하고 있다. ⚠️ 손 문서는 낡는다 (이미 한 번 낡았다). 자동 생성으로 바꿀지 정한다
      (8절).
- [ ] **`ddl-auto: update` 를 언제까지 쓸 것인가** — ⚠️ 마이그레이션 도구 결정(8절).
      운영 데이터가 생기기 전에 정해야 한다.

### ✅ 이번에 닫힌 것 (2026-09-11)

| 무엇 | 결과 |
| --- | --- |
| 프론트 주소가 두 곳에 리터럴 | `APP_FRONTEND_ORIGIN` **한 키**로. CORS 허용 출처와 로그인 리다이렉트가 같은 값에서 나온다 → [DATA_CONTRACT](docs/DATA_CONTRACT.md) §6 |
| 백엔드 콜백 주소도 리터럴 | `APP_OAUTH2_CALLBACK_BASE` 로 분리 — 프론트 주소와 **성격이 다른 값**이라 키를 따로 뒀다 |
| DB 접속 주소만 리터럴 | `DB_URL` 로. 계정·비번만 환경변수여서 「비밀은 뺐는데 접속은 내 PC 로 간다」였다 |
| 운영에서 SQL 전문이 찍힘 | `JPA_SHOW_SQL` 기본 **false**. 바인딩 값에 학생 이메일·답안이 들어 있었다 |
| 이미지에 개발 옵션 `--reload` | 제거. 🎯 소스를 마운트하지 않는 이미지라 **감시 비용만 내고 얻는 게 0** 이었다. 개발용은 compose 주석으로 옮겼다 |

### ✅ 지난 세션에 이미 닫혀 있던 것 (상자만 남아 있었다)

🔴 **체크 안 된 상자는 근거가 아니다** — 코드로 확인해 접었다 (2026-09-11).

| 무엇 | 실제 상태 |
| --- | --- |
| 적재 경로 하드코딩 | 이미 `DATA_PROBLEM_DIR`·`DATA_ANSWER_DIR` 로 빠져 있었다 (`6615ba4`) |
| `requirements.txt` 누락 | `pydantic-settings` 는 이미 명시돼 있다. `google-api-core` 는 **필요 자체가 없어졌다** — 제공자를 OpenAI 호환으로 바꿀 때 `ResourceExhausted` import 가 사라졌다 (`42a5c35`) |
| `CHAPTERS(grade,title)` 유니크 제약 | 이미 `UK_CHAPTERS_GRADE_TITLE` 로 걸려 있다 (`e22da4e`) |
| 문제 없는 챕터 2개 · `HTE_*` 표 5개 | 0절에 같은 항목이 있다 — 중복을 지우고 0절만 남겼다 |

### ✅ 적재에서 이번에 닫힌 것 (2026-09-02)

| 무엇 | 결과 |
| --- | --- |
| AI 문제 1건이 기출 적재를 영영 막음 | 가드를 `countBySourceType(REAL)` 로 |
| 실패가 `skipped` 하나로 뭉개짐 | 사유 6종으로 나눠 집계·보고 (`LoadOutcome`) |
| `Map.of` null 키 조회가 NPE | `HashMap` 으로 바꿔 가드가 «도달 가능» 해졌다 |
| `int` 라 필드 없음이 0(=하) 이 됨 | `Integer` 로. 없으면 중(MEDIUM), 경고를 남긴다 |
| 복수정답을 첫 건만 저장 | 전부 쉼표로 이어 저장 (실측 6건) |
| 적재 결과가 실행 환경마다 다름 | 파일을 이름순으로 읽는다 |
| `question_unit` 이 숫자가 아니면 문제를 버림 | 맨 뒤로 보내고 문제는 살린다 |
| 파일을 두 번 파싱 | `id` 는 이미 DTO 에 있다. 1,152건이면 2,304→1,152번 |

⚠️ **난이도 매핑은 일부러 안 건드렸다.** `표준`·`심화` 가 자료에 없어 상(HIGH) 이
안 나오는 것은 그대로다 — 결정 전에 바꾸면 지금 DB 와 또 어긋난다 (0절·8절).
대신 **점수로 떨어진 건수를 로그에 남긴다.**

## 5. 검사 — 🔴 지금 근거가 없다

🔄 **2026-09-15 기준 검사 241건 — 실제로 돌려서 전부 통과했다** (실패 0 · 오류 0 · 건너뜀 0).

```
Java (./gradlew test)                                           123건
  순수 로직                                                       69
    정규화 22 · 난이도 재계산 16 · 난이도 한 칸 7 · 난이도 폴백 4
    학년 4 · 학년 설정 요청 5 · 허용 횟수 경계 4 · 인증 메일 순서 7
  AI 서버 호출 (RestTemplate 을 가짜로)                           14
    실패 종류 보존 · 서비스 토큰 · 나가는 몸통에 개인정보 없음
  실제 DB 가 필요한 것                                             3
    동시 제출 2 · 원본 id 중복 차단 1
  인증 관문 (쿠키 토큰·로그아웃 블랙리스트)                       12
  경로별 인가 (관리자 차단 · 챕터 401/400)                         7
  학년 문자열 해석                                                17
  컨텍스트 로딩                                                    1

Python (ai-server, pytest)                                       78건
  응답 파싱·깨진 수식 복원                                        20
  저장 전 계약 (타입·빠진 항목·어휘·컬럼 한계·보기 번호)          25
  실패를 네 종류로 나누기 · 응답 본문 변환                        19
  경계와 상태 코드 (학생 토큰 차단 · 429/502/503)                 14

화면 (C:\webStorm_workspace\ailang, vitest)                      40건
  수식 구간 나누기                                                20
  실패를 읽는 자리 (상태 코드·문구·429 구분)                      10
  학년 어휘                                                        5
  로그인 상태                                                      5
```

🔬 **돌리는 법**: Java 는 `./gradlew test`, Python 은
[`ai-server/requirements-dev.txt`](ai-server/requirements-dev.txt) 맨 위 참고.
⚠️ 파이썬 가상환경은 **3.11 로** 만들 것 — 3.13 에서는 `requirements.txt` 설치가 깨진다
(langchain 이 끌고 오는 numpy 판에 3.13 휠이 없다). 이유와 우회법도 그 파일에 적어 뒀다.

🔴 **「JDK 가 없어서 못 돌린다」는 틀린 진단이었다.** 빌드 도구가 toolchain 으로
`~/.gradle/jdks/eclipse_adoptium-21-...` 에 이미 받아 뒀고, PATH 에만 없었다.
Docker 도 돌고 있다. **문서 줄을 확인 없이 믿은 것**이 원인이다.

🔴 **여전히 「덮였다」로 읽지 말 것.** 남은 구멍은 **적재(DataLoader)** 하나가 아니라 둘이다:
적재는 한 건도 안 재 봤고, **AI 왕복은 «계약» 만 덮었지 실제 모델의 응답은 못 봤다**
(일부러 그렇게 뒀다 — 진짜 모델을 부르는 검사를 만들지 않는 것이 규율이다).
그리고 **화면(프론트) 쪽 검사는 여전히 0건**이다.

- [ ] **`DataLoader` 두 번 실행** — ⚠️ **지금 구조로는 못 잰다.** 가드가
      「기출문제가 한 건이라도 있으면 통째로 건너뛴다」라서, 1,132건이 들어 있는 DB 에서는
      두 번째 실행이 아무 일도 안 한다. 진짜로 재려면 가드를 **원본 id 단위**로 바꿔야 하는데,
      그러면 **매 기동마다 JSON 2,300개를 다시 읽는다.** 비용을 맞바꾸는 설계 결정이라
      8절로 올린다.
- [ ] **화면의 «흐름» 은 아직 검사가 0건이다** — 순수 로직 40건은 붙었지만(2026-09-15),
      가입·풀이·채팅을 실제로 밟아 보는 검사는 없다. ⚠️ 컴포넌트 검사를 붙이려면
      `@testing-library/react` 가 필요하다 — 도구를 하나 더 들일지는 정해야 한다.
      🔬 돌리는 법: `cd C:\webStorm_workspace\ailang && npm test`
- [ ] **새 검사는 옛 코드에서 빨간불이어야 한다** — 양쪽 다 통과하면 아무것도 못 박은 게 없다.

### ✅ 이번에 닫힌 것 (2026-09-15)

| 무엇 | 결과 |
| --- | --- |
| 🔴 **검사를 한 번도 안 돌려 봤다** | 73건 전부 통과. 초록불을 본 사람이 생겼다 |
| 🔴 **동시 제출을 안 재 봤다** | 검사 2건. 그리고 **재 보자마자 결함이 나왔다** — 1절 참고 |
| `SOURCE_ID` 유일 제약을 아무도 확인 안 함 | 검사 1건. `unique = true` 를 빼면 빨간불이 된다 |
| 새 검사가 정말 무언가를 못 박는지 | **일부러 코드를 망가뜨려** 9건이 빨간불이 되는 것을 확인하고 되돌렸다 |
| 🔴 **파이썬 쪽 검사가 0건이었다** | **78건.** 응답 파싱 · 저장 전 계약 · 실패 종류 · 경계와 상태 코드 |
| 🔴 **AI 왕복을 한 번도 안 재 봤다** | Java 14건. `RestTemplate` 을 가짜로 바꿔 끼운다 — 진짜 모델은 부르지 않는다 |
| 🔴 **객관식 정답 검사가 「부분 문자열」이었다** | `not in "1234"` 라 `"12"`·`"123"`·`"1234"` 가 전부 통과했다. 보기 4개짜리 문제의 정답이 `"12"` 로 저장되면 학생은 무엇을 골라도 오답이다. 튜플 비교로 고쳤다 |
| 나가는 요청에 개인정보가 없는지 아무도 확인 안 함 | 검사 3건. **요청 DTO 에 필드가 늘면 빨간불**이 된다 — 늘어난 것이 개인정보인지 사람이 보게 만드는 것이 목적이다 |

## 6. Phase 3 — RAG ⏸ 1·2절 뒤

🔴 **지금 「구현돼 있다」고 읽힐 만한 흔적이 세 개 있는데 코드는 0줄이다:**
`requirements.txt` 의 `supabase`, `config.py` 가 **필수로 요구하는** Supabase 키 두 개,
그리고 `rag_service.py` 의 `# TODO` 한 줄.

- [ ] **AI Hub 수학 자료 전처리** — 텍스트 추출·정제·청킹(약 500 토큰)
- [ ] **Supabase pgvector 테이블 생성** — 스키마는 [`docs/DATA_CONTRACT.md`](docs/DATA_CONTRACT.md) §6 에 적는다
- [ ] **메타데이터 태깅** — `grade`, `chapter_title`
- [ ] **임베딩 적재 파이프라인** — 재실행해도 중복이 안 생기게
- [ ] **`rag_service.py` 검색 구현** — 학년 필터 + 코사인 유사도 → 프롬프트 주입
- [ ] **검색 결과가 없을 때의 동작 결정** — ⚠️ 빈 컨텍스트로 그냥 답하게 둘지, 「자료 없음」을
      밝힐지. 근거 없는 답을 근거 있는 답처럼 보이게 하면 안 된다.

## 7. Phase 4 — 관리자 · 마무리 ⏸

- [ ] 관리자 챕터 CRUD (`/api/admin/chapters`)
- [ ] 관리자 문제 CRUD (`/api/admin/problems`)
- [ ] AI 생성 문제 검수 화면 — `SOURCE_TYPE = 'AI'` 인 문제의 정답을 사람이 확인하는 자리
- [ ] 전체 통합 테스트
- [ ] 배포 대상 결정 — ⚠️ 아래 8절

---

## 8. 결정 대기 — 🔴 혼자 정하지 말 것

| 항목 | 왜 지금 못 정하나 |
| --- | --- |
| **세션 설계 한 덩어리** 🔴 | ① 토큰 종류(`typ`) ② 한 계정 몇 기기 ③ 서비스 호출 신원 — **셋이 같은 결정**이다. 지금은 리프레시 토큰이 이메일 하나로만 저장돼 **두 번째 기기 로그인이 첫 기기를 튕겨낸다.** 의도인지부터 정한다 |
| **단답형 채점 방식** | 자가채점을 유지할지, 서버 채점 + 이의제기로 바꿀지. 학습 경험의 문제라 사용자가 정한다. ⚠️ 진단 테스트 흐름이 여기 의존한다(`Assessment.tsx`) |
| **인증코드 잠금 횟수** | 「몇 번 틀리면 잠그나」·「한 주소에 몇 통까지」. 🔄 2026-09-15 부터 **잠정값(창 1시간 5통 · 코드당 5회)으로 돌고 있다** — 무제한으로 두지 않으려는 임시값이다. 설정이라 재배포 없이 바뀐다 |
| **객관식 14문제의 정답 칸에 풀이가 들어 있다** 🔴 | 500문제 중 14문제. 풀이를 **버릴지**, **해설 칸으로 옮길지**. 채점 방식과 얽힌 판단이다 (0절) |
| **적재 가드를 원본 id 단위로 바꿀 것인가** | 지금은 「한 건이라도 있으면 통째로 건너뛴다」라 **절반만 들어간 상태를 채울 수 없다.** 바꾸면 이어서 채울 수 있지만 **매 기동마다 JSON 2,300개를 다시 읽는다.** 적재 멱등성 검사가 여기 달려 있다 (5절) |
| **학년을 바꾸면 진단을 다시 받아야 하나** | 지금은 학년만 바꾸고 **진단 완료 표시는 그대로 둔다.** 자동으로 끄면 학년을 잘못 골랐다 되돌린 학생의 진단까지 지워지고, 안 끄면 초6 진단 결과가 중1 학생에게 남는다 |
| **진단 테스트 결과를 초기 난이도에 쓸 것인가** | 지금은 제출이 챕터 통계에 흩어져 들어가 `totalCount < 3` 에 머문다 — 난이도는 안 움직이고 **정답률 분모만 오염된다.** 초기값으로 쓸지, 통계에서 뺄지 |
| **스키마 마이그레이션 도구** | `ddl-auto: update` 를 언제까지 쓸지. 운영 데이터가 생기기 전에 정해야 한다 |
| **배포 대상** | compose 에 스프링이 없다. 어디에 띄울지가 정해져야 포트·오리진·시크릿 관리가 정해진다 |
| **AI 생성 문제의 검수 절차** | 사람이 볼지, 규칙으로 거를지, 학생에게 「AI 문제」라고 표시할지 |
| **RAG 자료 출처와 라이선스** | AI Hub 자료를 벡터 저장소에 넣어도 되는 범위 |

---

## ✅ 끝난 것

| 무엇 | 결과 | 정본 |
| --- | --- | --- |
| 인증 기반 (Phase 1) | 이메일 인증·JWT·구글 OAuth2·LOCAL↔GOOGLE 중복 차단·로그아웃 블랙리스트 | [docs/API_CONTRACT.md](docs/API_CONTRACT.md) §1 |
| 문제 풀이 핵심 (Phase 2) | 챕터·문제·이력·통계 4개 표, 맞춤/랜덤/학년별/진단 문제, 제출·난이도 재계산 | [docs/DATA_CONTRACT.md](docs/DATA_CONTRACT.md) §2 |
| AI 연동 | 개념 설명·모의문제·챗봇 3개 엔드포인트, Gemini 할당량 예외 처리(FastAPI 쪽) | [docs/rules/ai-call-policy.md](docs/rules/ai-call-policy.md) |
| 기출문제 적재 | AI Hub JSON → `PROBLEMS`, 챕터 자동 생성, 원문자 정답 정규화 스크립트 | [docs/research/aihub-json-structure.md](docs/research/aihub-json-structure.md) |
| 문서·규율 체계 (2026-08-27) | `commerce-signal` 관례 이식, `PROJECT_PLANNER.md` 를 4개 정본으로 분리 | [docs/00_CODE_WALKTHROUGH.md](docs/00_CODE_WALKTHROUGH.md) |
